# Source code

Currently the full pipeline lives in `notebooks/intraday_arbitrage.ipynb`.
Future refactor: extract feature engineering, model training, and backtest
logic into reusable modules here (`features.py`, `model.py`, `backtest.py`).
